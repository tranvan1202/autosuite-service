# Base runtime deps
-r requirements.txt

# Test runner (modern, stable)
pytest==8.4.2
# Parallel test execution (speed on CI)
pytest-xdist>=3.6
# Coverage plugin for pytest (consistent reports)
pytest-html>=4.1.1
pytest-cov>=4.1
# Time freezing for deterministic tests
freezegun>=1.5
# Allure integration (optional reporting)
allure-pytest==2.15.0

# Linting (fast, batteries included)
ruff==0.14.1
# Code formatter (consistent style)
black==25.9.0
# Static typing (pin for stability on CI)
mypy==1.11.2

# Coverage engine (explicit install for local runs & CI gates)
coverage>=7.6
