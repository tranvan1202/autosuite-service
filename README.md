# autosuite-service
A lightweight automation service that runs browser tasks using FastAPI and Playwright. It follows a clean structure with Page Objects, background job handling, and reliable CI checks.
